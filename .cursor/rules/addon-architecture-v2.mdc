---
description: 扩展包架构 V2 设计方案 — 路径隔离 + 注册机制
alwaysApply: false
globs:
  - server/addons/**
  - server/internal/addon/**
  - server/internal/cmdtools/addon/**
  - web/src/addons/**
---

# 扩展包架构 V2 — 设计方案

> 本文档描述 XYGo Admin 扩展包体系的重新设计。核心目标：**扩展与内核在文件系统层面物理隔离**，
> 系统升级包永远不会触碰扩展代码，扩展开发者有明确的开发边界和统一规范。

## 一、现状问题

当前扩展包（addon ZIP）安装时将 `files/` 下的文件按任意相对路径覆盖到项目根目录。
这导致扩展文件散落在核心目录（如 `server/api/admin/`、`server/internal/controller/admin/`、
`server/internal/service/` 等），与系统内核代码混在一起。

**后果**：

1. 系统升级包与扩展文件互相干扰，checksums 冲突
2. 磁盘上无法区分哪些文件属于核心、哪些属于扩展
3. 扩展开发者没有统一规范，可以往任意目录塞文件
4. 卸载依赖 `file_list` JSON 记录，不可靠

## 二、核心原则

```
┌─────────────────────────────────────────────────┐
│  系统升级包 → 只操作「核心区域」                    │
│  扩展安装器 → 只操作「扩展区域」                    │
│  两个区域在文件系统层面完全隔离，永不交叉            │
└─────────────────────────────────────────────────┘
```

**核心区域**（受升级包保护）：

- `server/api/`（不含 `server/addons/`）
- `server/internal/`（不含 `server/addons/`）
- `web/src/views/`（不含 `web/src/addons/`）

**扩展区域**（升级包永不触碰）：

- `server/addons/{name}/`
- `web/src/addons/{name}/`
- `web-{name}/`（独立前端应用，如 `web-tenant/`）

## 三、扩展包目录结构

### 3.1 ZIP 包结构（新版）

```
{name}.zip
├── addon.yaml                     # 扩展元信息
├── install/
│   ├── pgsql.sql                  # 建表 + 插入菜单 + 初始数据
│   └── mysql.sql
├── upgrade/                       # 增量升级 SQL（可选）
│   └── pgsql.sql
├── uninstall/
│   ├── pgsql.sql                  # 删表 + 删除菜单 + 清理数据
│   └── mysql.sql
├── server/                        # → 安装到 server/addons/{name}/
│   ├── api/                       # API 请求/响应定义
│   ├── controller/                # 控制器
│   ├── logic/                     # 业务逻辑
│   ├── model/                     # 模型定义
│   ├── service/                   # 服务接口
│   ├── crons/                     # 定时任务（可选）
│   ├── queues/                    # 消息队列消费者（可选）
│   └── module.go                  # 模块注册入口（路由 + WS + Queue + Cron）
├── web/                           # → 安装到 web/src/addons/{name}/
│   ├── views/                     # Vue 页面组件
│   ├── api/                       # 前端 API 请求封装
│   └── manifest.json              # 声明菜单贡献、组件路径映射
└── web-{name}/                    # → 安装到 web-{name}/（独立前端，可选）
    └── ...
```

### 3.2 安装路径映射规则

| ZIP 内路径 | 安装到磁盘位置 | 说明 |
|-----------|--------------|------|
| `server/` | `server/addons/{name}/` | 后端代码，独立 Go 包 |
| `web/` | `web/src/addons/{name}/` | 前端组件 |
| `web-{name}/` | `web-{name}/` | 独立前端应用 |
| `install/` | 不复制到磁盘，仅执行 SQL | 安装时运行 |
| `uninstall/` | 不复制到磁盘，仅执行 SQL | 卸载时运行 |
| `addon.yaml` | `server/addons/{name}/addon.yaml` | 保留元信息 |

### 3.3 addon.yaml 规范

```yaml
name: tenant                       # 扩展唯一标识，与 ZIP 文件名一致
version: "1.0.0"                   # 语义化版本号
title: "多租户管理"                  # 显示名称
description: "多租户 SaaS 模式支持"  # 描述
author: "XYGo Team"
min_version: "1.3.0"               # 要求的最低系统版本

# 声明使用的系统扩展点
features:
  routes: true                     # 注册 HTTP 路由
  websocket: true                  # 注册 WebSocket 事件处理器
  queue: true                      # 注册消息队列消费者
  cron: true                       # 注册定时任务
  menus:                           # 菜单声明（安装/卸载时由 SQL 管理）
    admin:                         # 后台菜单
      - parent: "system"           # 挂载到的父菜单 name
        items:
          - name: "TenantList"
            title: "租户管理"
            path: "tenant/list"
            component: "@addons/tenant/views/list"
            icon: "ri:building-line"
    member:                        # 前台会员菜单（可选）
      - name: "TenantPortal"
        title: "租户入口"
        path: "/tenant"
        component: "@addons/tenant/views/portal"

# 独立前端声明（可选）
independent_web:
  - name: web-tenant
    description: "租户管理端独立前端"
```

## 四、后端注册机制

### 4.1 核心注册表（新增 `server/internal/addon/`）

系统核心提供一个极简的注册表模块：

```go
// server/internal/addon/registry.go
package addon

import "github.com/gogf/gf/v2/net/ghttp"

type Module struct {
    Name     string
    Mount    func(s *ghttp.Server)  // 路由注册
}

var modules []Module

func Register(m Module) {
    modules = append(modules, m)
}

func MountAll(s *ghttp.Server) {
    for _, m := range modules {
        if m.Mount != nil {
            m.Mount(s)
        }
    }
}
```

### 4.2 扩展模块入口（每个扩展必须实现）

每个扩展包有一个 `module.go` 作为统一入口，在 `init()` 中完成所有注册。

**普通 addon**（无独立前台，纯后台管理）：

```go
// server/addons/shop/module.go
package shop

import (
    "xygo/addons/shop/controller"
    "xygo/internal/addon"
    "xygo/internal/middleware"
    "github.com/gogf/gf/v2/net/ghttp"
)

func init() {
    addon.Register(addon.Module{
        Name: "shop",
        Mount: func(s *ghttp.Server) {
            s.Group("/", func(group *ghttp.RouterGroup) {
                group.Middleware(middleware.CORS, middleware.ResponseHandler)
                group.Group("/", func(ag *ghttp.RouterGroup) {
                    ag.Middleware(middleware.AdminAuth, middleware.DemoGuard)
                    ag.Bind(controller.NewV1())
                })
            })
        },
    })
}
```

**含独立前台的 addon**（如 tenant，有平台管理端 + 自身前台端）：

```go
// server/addons/tenant/module.go
package tenant

import (
    "xygo/addons/tenant/controller"
    "xygo/internal/addon"
    "xygo/internal/middleware"
    "xygo/internal/websocket"
    "github.com/gogf/gf/v2/net/ghttp"
)

func init() {
    addon.Register(addon.Module{
        Name:  "tenant",
        Mount: mountRoutes,
    })
}

func mountRoutes(s *ghttp.Server) {
    // 路由组 1：平台管理端 — 复用核心 AdminAuth
    s.Group("/", func(group *ghttp.RouterGroup) {
        group.Middleware(middleware.CORS, middleware.SlowApiMonitor, middleware.ResponseHandler)
        group.Group("/", func(ag *ghttp.RouterGroup) {
            ag.Middleware(middleware.AdminAuth, middleware.DemoGuard, middleware.OperationLog)
            ag.Bind(controller.NewAdminV1())  // admin_*.go 中的方法
        })
    })

    // 路由组 2：Addon 自身端 — 独立鉴权
    s.Group("/", func(group *ghttp.RouterGroup) {
        group.Middleware(middleware.CORS, middleware.ResponseHandler)
        group.Middleware(tenantResolve, tenantAdminAuth, middleware.DemoGuard)
        group.Bind(controller.NewV1())        // 其余方法
    })

    // WebSocket（如需要）
    s.Group("/tenant-socket", func(group *ghttp.RouterGroup) {
        group.Middleware(middleware.CORS, wsTenantAuth)
        group.GET("/", websocket.WsHandler)
    })
}
```

关于双控制器的详细约定见 [4.3 节](#43-双控制器约定含独立前台的-addon)。

### 4.3 双控制器约定（含独立前台的 addon）

部分 addon（如 tenant）同时提供两类接口：

1. **平台管理接口**：平台管理员操作（如管理租户列表、套餐分配），路由前缀 `/admin/`，走核心 `AdminAuth` 中间件
2. **Addon 自身接口**：addon 用户操作（如租户管理员登录、管理部门角色），路由前缀 `/{name}/`，走 addon 自身鉴权中间件

对于这类 addon，**必须**遵循以下约定：

#### 4.3.1 controller 层：双 struct 分离

```go
// controller/controller.go
package controller

type AdminControllerV1 struct{}   // 平台管理端 — 走 AdminAuth
type ControllerV1 struct{}        // Addon 自身端 — 走 addon 鉴权

func NewAdminV1() *AdminControllerV1 { return &AdminControllerV1{} }
func NewV1() *ControllerV1           { return &ControllerV1{} }
```

#### 4.3.2 文件命名规则

| 文件名前缀 | Controller Receiver | 路由鉴权 | 示例 |
|-----------|---------------------|---------|------|
| `admin_*.go` | `*AdminControllerV1` | 核心 `AdminAuth` | `admin_tenant.go`, `admin_tenant_menu.go` |
| 其他（无 `admin_` 前缀） | `*ControllerV1` | addon 自身鉴权 | `user.go`, `dept.go`, `chat.go` |

**严格约束**：`admin_*.go` 文件中的方法 receiver **禁止**使用 `*ControllerV1`，否则会导致平台管理接口走错鉴权中间件。

#### 4.3.3 api 层同样遵循前缀规则

| 文件名 | 路由路径前缀 | 说明 |
|--------|------------|------|
| `api/admin_*.go` | `/admin/{module}/` | 平台管理员操作 |
| `api/{name}_*.go` | `/{name}/` | addon 用户操作 |

#### 4.3.4 module.go 路由注册

```go
func mountRoutes(s *ghttp.Server) {
    // 路由组 1：平台管理端 — AdminAuth
    s.Group("/", func(group *ghttp.RouterGroup) {
        group.Middleware(middleware.CORS, middleware.ResponseHandler)
        group.Group("/", func(ag *ghttp.RouterGroup) {
            ag.Middleware(middleware.AdminAuth, middleware.DemoGuard)
            ag.Bind(controller.NewAdminV1())   // admin_*.go 中的方法
        })
    })

    // 路由组 2：Addon 自身端 — addon 自定义鉴权
    s.Group("/", func(group *ghttp.RouterGroup) {
        group.Middleware(middleware.CORS, middleware.ResponseHandler)
        group.Middleware(addonResolve, addonAuth)
        group.Bind(controller.NewV1())         // 其余方法
    })
}
```

> **不含独立前台的普通 addon**（如只在平台后台管理的 shop 模块）只需要 `ControllerV1` + `NewV1()` + `AdminAuth`，无需双控制器。

### 4.4 自动生成的导入文件

addon installer 自动维护一个聚合导入文件，模式与 GoFrame `gf gen service` 生成的
`internal/logic/logic.go` 完全一致：

```go
// server/addons/addons.go
// Code generated and maintained by addon installer. DO NOT EDIT.
package addons

import (
    _ "xygo/addons/tenant"
    // 安装新扩展时自动追加，卸载时自动移除
)
```

`main.go` 中的导入（与 logic、crons、queues 同级）：

```go
import (
    _ "xygo/internal/logic"   // GoFrame gf gen service 自动维护
    _ "xygo/internal/crons"   // 定时任务
    _ "xygo/internal/queues"  // 消息队列
    _ "xygo/addons"           // 扩展模块（addon installer 自动维护）
)
```

### 4.5 已有系统能力的扩展点总结

| 系统能力 | 注册 API | 扩展方式 | 说明 |
|---------|---------|---------|------|
| HTTP 路由 | `addon.Register(Module{...})` | `init()` 中注册 | 新增，专为扩展设计 |
| WebSocket | `websocket.RegisterEvent(event, handler)` | `init()` 中注册 | 已有，无需改动 |
| 消息队列 | `queue.Register(consumer)` | `init()` 中注册 | 已有，无需改动 |
| 定时任务 | `cron.Register(task)` | `init()` 中注册 | 已有，无需改动；调度配置仍走数据库 |
| 菜单管理 | SQL INSERT/DELETE | `install/uninstall` SQL | 安装时插入菜单，卸载时删除 |

> **关键发现**：WebSocket、Queue、Cron 三个模块已经使用 `init()` + 注册表模式，
> 与本方案完全兼容。扩展只需在 `module.go` 的 `init()` 中调用即可，零改动核心代码。

## 五、前端集成

### 5.1 ComponentLoader 扩展

`web/src/router/core/ComponentLoader.ts` 增加对 addons 目录的组件扫描：

```typescript
constructor() {
    this.modules = import.meta.glob('../../views/**/*.vue')
    // 新增：扫描扩展目录
    this.addonModules = import.meta.glob('../../addons/*/views/**/*.vue')
}
```

组件路径约定：

- 核心组件：`/backend/system/user` → `views/backend/system/user/index.vue`
- 扩展组件：`@addons/tenant/views/list` → `addons/tenant/views/list/index.vue`

`ComponentLoader.load()` 增加 `@addons/` 前缀判断：

```typescript
load(componentPath: string): () => Promise<any> {
    // 扩展组件：@addons/tenant/views/xxx
    if (componentPath.startsWith('@addons/')) {
        return this.loadAddonComponent(componentPath)
    }
    // ... 原有逻辑不变
}

private loadAddonComponent(componentPath: string): (() => Promise<any>) | null {
    const addonPath = componentPath.replace('@addons/', '')
    const paths = [
        `../../addons/${addonPath}.vue`,
        `../../addons/${addonPath}/index.vue`
    ]
    for (const path of paths) {
        if (this.addonModules[path]) {
            return this.addonModules[path]
        }
    }
    return this.createErrorComponent(componentPath)
}
```

### 5.2 扩展前端 manifest.json

每个扩展的前端部分可包含 `manifest.json` 声明元信息：

```json
{
    "name": "tenant",
    "title": "多租户管理",
    "version": "1.0.0",
    "components": {
        "tenant/list": "views/list/index.vue",
        "tenant/group": "views/group/index.vue"
    }
}
```

> 菜单数据仍然由 SQL 写入数据库（`xy_admin_menu`），`component` 字段使用
> `@addons/tenant/views/list` 格式，ComponentLoader 自动解析。

### 5.3 扩展前端 API 请求

扩展的 API 请求封装放在 `web/src/addons/{name}/api/` 下：

```typescript
// web/src/addons/tenant/api/tenant.ts
import { adminRequest } from '@/utils/http'

export function getTenantList(params: any) {
    return adminRequest.get('/admin/tenant/list', { params })
}
```

## 六、菜单生命周期管理

### 6.1 安装时添加菜单

菜单通过 `install/pgsql.sql` 中的 INSERT 语句添加。规范要求：

```sql
-- 使用固定 ID 段（每个扩展分配一个 ID 范围，避免冲突）
-- 或使用序列自增（推荐，更安全）
-- 所有扩展菜单的 remark 字段标记来源，便于识别和卸载

-- 父菜单
INSERT INTO xy_admin_menu (parent_id, type, title, name, path, component, icon, sort, status, remark, create_time, update_time)
VALUES (0, 2, '租户管理', 'TenantManage', 'tenant', '@addons/tenant/views/list', 'ri:building-line', 50, 1,
        'addon:tenant', EXTRACT(EPOCH FROM NOW())::int, EXTRACT(EPOCH FROM NOW())::int);

-- 子菜单使用 currval 引用父菜单 ID
INSERT INTO xy_admin_menu (parent_id, type, title, name, path, component, icon, sort, status, remark, create_time, update_time)
VALUES (currval('xy_admin_menu_id_seq'), 2, '租户列表', 'TenantList', 'list', '@addons/tenant/views/list', '', 1, 1,
        'addon:tenant', EXTRACT(EPOCH FROM NOW())::int, EXTRACT(EPOCH FROM NOW())::int);
```

**关键约定**：`remark` 字段写入 `addon:{name}` 标记。

### 6.2 卸载时删除菜单

`uninstall/pgsql.sql` 利用 remark 标记批量删除：

```sql
DELETE FROM xy_admin_menu WHERE remark = 'addon:tenant';
```

这比按 ID 删除更可靠，不依赖写死的 ID 值。

### 6.3 前台会员菜单（如需要）

同理在 `xy_member_menu` 中 INSERT/DELETE，`remark` 同样标记 `addon:{name}`。

## 七、升级包保护

### 7.1 pack.py SKIP_PATTERNS 更新

```python
SKIP_PATTERNS = [
    # ... 原有规则
    'server/addons/',          # 扩展后端代码
    'web/src/addons/',         # 扩展前端代码
    'web-tenant/',             # 独立前端应用（示例）
]
```

### 7.2 updater.go 保护

updater 在覆盖文件前，检查路径是否在扩展区域内，如果是则跳过：

```go
addonPrefixes := []string{"server/addons/", "web/src/addons/"}
for _, prefix := range addonPrefixes {
    if strings.HasPrefix(relPath, prefix) {
        continue // 不覆盖扩展区域
    }
}
```

## 八、改造后的 addon installer

### 8.1 安装流程（新）

```
[1/9] 解压 ZIP 到临时目录
[2/9] 读取 addon.yaml，校验 min_version
[3/9] 检查安装状态（新装 / 升级 / 覆盖 / MinUpgradeFrom 校验）
[4/9] 执行 install（或 upgrade）SQL
[5/9] 安装扩展菜单（从 addon.yaml 声明式写入）
[6/9] 备份旧版本文件（升级时 → .backup/{name}-{oldVer}/）
[7/9] 复制文件到隔离目录：
      server/ → server/addons/{name}/
      web/    → web/src/addons/{name}/
      web-{name}/ → web-{name}/
[8/9] 更新 addons/addons.go（扫描 addons/ 重新生成空导入）
[9/9] 记录安装信息到 xy_addon
```

### 8.2 卸载流程（新）

```
[1/6] 确认卸载
[2/6] 执行 uninstall SQL（删表等）
[3/6] 卸载扩展菜单（按 remark='addon:{name}' 批量删除）
[4/6] 删除整个目录：
      rm -rf server/addons/{name}/
      rm -rf web/src/addons/{name}/
      rm -rf web-{name}/（如果存在）
[5/6] 更新 addons/addons.go（扫描 addons/ 重新生成空导入）
[6/6] 更新 xy_addon 状态
```

**卸载的优势**：不再逐文件删除，直接 `os.RemoveAll` 整个扩展目录，100% 干净。

### 8.3 addons/addons.go 自动维护

安装器扫描 `server/addons/*/module.go`，自动重新生成聚合导入文件，
模式与 GoFrame 的 `gf gen service` 生成 `logic.go` 完全一致：

```go
// 伪代码
func updateAddonsImport() {
    addons := scanInstalledAddons("addons/")  // 扫描有 module.go 的子目录
    imports := []string{}
    for _, name := range addons {
        imports = append(imports, fmt.Sprintf(`    _ "xygo/addons/%s"`, name))
    }
    var importBlock string
    if len(imports) > 0 {
        importBlock = fmt.Sprintf("\nimport (\n%s\n)\n", strings.Join(imports, "\n"))
    }
    content := "// Code generated and maintained by addon installer. DO NOT EDIT.\n\npackage addons\n" + importBlock
    writeFile("addons/addons.go", content)
}
```

## 九、安全检测机制

### 9.1 数据库表命名约束

**规则**：扩展创建的所有表必须以 `xy_{addon_name}_` 为前缀。

安装器在执行 SQL 前进行静态检查：

```
✅ 允许: CREATE TABLE xy_shop_order ...
✅ 允许: CREATE INDEX idx_shop_order_status ON xy_shop_order ...
✅ 允许: DROP TABLE IF EXISTS xy_shop_order         (仅在 uninstall SQL 中)
✅ 允许: INSERT INTO xy_admin_menu ... remark='addon:shop'
✅ 允许: DELETE FROM xy_admin_menu WHERE remark='addon:shop'
✅ 允许: INSERT INTO xy_sys_config ... group='addon_shop'
❌ 拒绝: CREATE TABLE xy_order ...                  (缺少扩展名前缀)
❌ 拒绝: ALTER TABLE xy_admin_user ...               (修改核心表)
❌ 拒绝: DROP TABLE xy_sys_config                    (删除核心表)
❌ 拒绝: DELETE FROM xy_admin_menu WHERE id=123      (未使用 remark 标记)
❌ 拒绝: UPDATE xy_admin_menu SET ...                (不允许修改现有菜单)
```

**实现方式**：安装器解析 SQL 语句，提取表名，对照白名单/黑名单校验。

系统核心表黑名单（禁止 ALTER/DROP/UPDATE/DELETE）：

```go
var systemTables = []string{
    "xy_admin_user", "xy_admin_role", "xy_admin_menu",
    "xy_admin_dept", "xy_admin_post", "xy_admin_role_menu",
    "xy_sys_config", "xy_sys_cron",
    "xy_member", "xy_member_menu", "xy_member_group",
    // ... 所有核心表
}
```

扩展只允许对以下表进行写操作（有条件）：

| 目标表 | 允许的操作 | 条件 |
|-------|----------|------|
| `xy_{name}_*` | CREATE/DROP/ALTER/INSERT/UPDATE/DELETE | 表名前缀必须匹配 |
| `xy_admin_menu` | INSERT/DELETE | 必须带 `remark='addon:{name}'` |
| `xy_member_menu` | INSERT/DELETE | 必须带 `remark='addon:{name}'` |
| `xy_sys_config` | INSERT/DELETE | 必须带 `group='addon_{name}'` |
| `xy_addon` | 无（安装器自行管理） | — |

### 9.2 菜单冲突检测

安装器在执行菜单 INSERT 前，自动检查冲突：

```go
func checkMenuConflict(ctx context.Context, db gdb.DB, addonName string, menuSQL string) error {
    // 1. 解析 SQL 中所有 INSERT INTO xy_admin_menu 的 name 和 path 值
    // 2. 查询数据库中是否已存在同名菜单
    existing, _ := db.GetOne(ctx,
        "SELECT id, name, remark FROM xy_admin_menu WHERE name=? AND deleted_at=0", menuName)
    if existing != nil {
        ownerAddon := existing["remark"].String()
        if ownerAddon != "" && ownerAddon != "addon:"+addonName {
            return fmt.Errorf("菜单 name='%s' 已被扩展 %s 占用", menuName, ownerAddon)
        }
        if ownerAddon == "" {
            return fmt.Errorf("菜单 name='%s' 是系统核心菜单，不允许覆盖", menuName)
        }
    }
    // 3. 检查同一 parent 下是否有 path 冲突
    // ...
}
```

检查项：

| 检查 | 说明 |
|------|------|
| `name` 唯一性 | 菜单 `name` 字段全局唯一，与核心菜单和其他扩展不能重复 |
| `path` 唯一性 | 同一 `parent_id` 下 `path` 不能重复 |
| `remark` 标记 | 所有扩展菜单的 `remark` 必须为 `addon:{name}`，否则拒绝 |
| `perms` 前缀 | 按钮权限标识建议以 `/admin/{name}/` 开头，避免与核心权限冲突 |

### 9.3 菜单卸载保障

卸载时双重保障：

1. **uninstall SQL**：`DELETE FROM xy_admin_menu WHERE remark = 'addon:{name}'`
2. **安装器兜底**：即使 uninstall SQL 执行失败或遗漏，安装器自动执行清理：

```go
func cleanupAddonMenus(ctx context.Context, db gdb.DB, addonName string) {
    tag := "addon:" + addonName
    // 先删子菜单（type=3 按钮），再删页面菜单（type=2），避免外键/子节点检查
    db.Exec(ctx, "DELETE FROM xy_admin_menu WHERE remark=? AND type=3", tag)
    db.Exec(ctx, "DELETE FROM xy_admin_menu WHERE remark=? AND type=2", tag)
    db.Exec(ctx, "DELETE FROM xy_admin_menu WHERE remark=?", tag)
    // 同理清理前台菜单
    db.Exec(ctx, "DELETE FROM xy_member_menu WHERE remark=?", tag)
}
```

### 9.4 配置项隔离

扩展如需在系统配置表中存储配置（`xy_sys_config`），规则：

- `group` 字段必须为 `addon_{name}`（如 `addon_shop`）
- `key` 字段建议以扩展名为前缀（如 `shop_auto_confirm_days`）
- 安装器检查 group 是否已被占用

```sql
-- install SQL 中插入扩展配置
INSERT INTO xy_sys_config ("group", group_name, name, key, value, type, sort, remark)
VALUES
    ('addon_shop', '商城配置', '自动确认天数', 'shop_auto_confirm_days', '7', 'input', 1, 'addon:shop'),
    ('addon_shop', '商城配置', '库存预警阈值', 'shop_stock_warning', '10', 'input', 2, 'addon:shop');
```

卸载时清理：

```sql
DELETE FROM xy_sys_config WHERE "group" = 'addon_shop';
```

安装器检查逻辑：

```go
func checkConfigConflict(ctx context.Context, db gdb.DB, addonName string) error {
    group := "addon_" + addonName
    existing, _ := db.GetOne(ctx,
        "SELECT key FROM xy_sys_config WHERE \"group\"=? LIMIT 1", group)
    if existing != nil {
        // group 已存在，可能是重装，检查是否属于同一扩展
    }
    // 检查 key 是否与其他 group 冲突
    return nil
}
```

**前端配置页面**：扩展的配置项自动出现在「系统配置」页面的独立分组标签下
（因为系统配置页已按 `group` 分组渲染，`addon_shop` 会自动成为一个新标签页）。

## 十、核心能力开放 — Auth/JWT 工厂

### 10.1 问题

当扩展开发者想创建一个新的独立端（如供应商端、代理商端），他需要：
- 独立的 JWT 签发/解析
- 独立的鉴权中间件
- 独立的 Token 缓存前缀
- 独立的登录/登出 API

当前实现中，每个端（admin、member、tenant）的 JWT 和 Auth 都是**独立手写的函数**
（`GenerateAdmin`、`ParseAdmin`、`GenerateTenant`、`ParseTenant` 等），不可复用。
新增一个端就要去改 `token/jwt.go`、`token/tenant.go`、`middleware/` 等核心包。

### 10.2 方案：Auth Endpoint 工厂

将 token 库重构为**可参数化创建**的模式，扩展可以用工厂函数创建自己的认证端点：

```go
// server/internal/library/token/endpoint.go (核心提供)

type EndpointConfig struct {
    App         string // 应用标识，如 "supplier"
    TokenHeader string // HTTP Header 名，如 "Xy-Supplier-Token"，留空则用 Authorization
    ConfigKey   string // 配置前缀，如 "auth.supplier" → 从 config.yaml 或 sys_config 读取
    MultiLogin  bool   // 是否允许多端登录
}

type Endpoint struct {
    config EndpointConfig
    // ...
}

// NewEndpoint 创建一个新的认证端点
func NewEndpoint(cfg EndpointConfig) *Endpoint {
    return &Endpoint{config: cfg}
}

// Generate 签发 Token
func (e *Endpoint) Generate(ctx context.Context, claims jwt.Claims) (string, int64, error) {
    // 使用 e.config.App 作为缓存前缀，实现 Token 隔离
}

// Parse 解析 Token
func (e *Endpoint) Parse(ctx context.Context, tokenStr string) (jwt.Claims, error) {
    // ...
}

// Middleware 生成鉴权中间件
func (e *Endpoint) Middleware(whitelist ...string) func(r *ghttp.Request) {
    return func(r *ghttp.Request) {
        // 从 e.config.TokenHeader 或 Authorization 读取 token
        // 调用 e.Parse 验证
        // 注入上下文
    }
}
```

### 10.3 扩展使用示例

```go
// server/addons/supplier/module.go
package supplier

import (
    "xygo/internal/addon"
    "xygo/internal/library/token"
)

var supplierAuth = token.NewEndpoint(token.EndpointConfig{
    App:         "supplier",
    TokenHeader: "Xy-Supplier-Token",
    ConfigKey:   "addon_supplier",  // 从 sys_config group=addon_supplier 读 JWT 配置
    MultiLogin:  true,
})

func init() {
    addon.Register(addon.Module{
        Name: "supplier",
        Mount: func(s *ghttp.Server) {
            s.Group("/supplier", func(group *ghttp.RouterGroup) {
                group.Middleware(supplierAuth.Middleware("/supplier/auth/login"))
                group.Bind(controller.NewV1())
            })
        },
    })
}
```

### 10.4 向后兼容

现有的 `GenerateAdmin`、`ParseAdmin`、`AdminAuth` 等不需要立即重构。
可以逐步将它们改为基于 `Endpoint` 的实现：

```go
// token/jwt.go 中
var adminEndpoint = NewEndpoint(EndpointConfig{
    App: AppAdmin, TokenHeader: "", ConfigKey: "auth.jwt", MultiLogin: false,
})

func GenerateAdmin(ctx context.Context, user model.AuthUser) (string, int64, error) {
    return adminEndpoint.Generate(ctx, &Claims{AuthUser: &user, ...})
}
```

现有代码调用方式不变，但内部实现统一走 `Endpoint`。

### 10.5 独立前端的 Auth 对接

扩展如果有独立前端（如 `web-supplier/`），其登录流程：
1. 前端调用 `/supplier/auth/login` 获取 token
2. 后续请求在 Header 中携带 `Xy-Supplier-Token: xxx`
3. `supplierAuth.Middleware()` 自动校验

前端 `.env` 中配置 API 前缀和 Token Header 名即可，
与现有 `web-tenant/` 的模式一致。

## 十一、扩展开发者规范

### 11.1 目录规范

```
server/addons/{name}/
├── module.go                  # [必须] init() 入口，注册路由/WS/Queue/Cron
├── addon.yaml                 # [必须] 元信息
├── api/                       # [推荐] API 请求/响应结构定义
├── controller/                # [推荐] 控制器
├── logic/                     # [推荐] 业务逻辑
├── model/                     # [可选] 输入/输出模型
├── service/                   # [可选] 服务接口
├── crons/                     # [可选] 定时任务实现
├── queues/                    # [可选] 队列消费者实现
└── README.md                  # [推荐] 扩展说明
```

### 11.2 命名规范

| 对象 | 规则 | 示例 |
|------|------|------|
| 扩展名 | 小写英文，单词用下划线分隔 | `shop`、`live_stream` |
| 数据库表 | `xy_{addon_name}_` 前缀 | `xy_shop_order` |
| 菜单 name | PascalCase，以扩展名开头 | `ShopOrderList` |
| 菜单 remark | `addon:{name}` | `addon:shop` |
| 配置 group | `addon_{name}` | `addon_shop` |
| 配置 key | 扩展名前缀 | `shop_auto_confirm_days` |
| API 路径 | `/{endpoint}/{name}/...` | `/admin/shop/order/list` |
| 权限标识 | `/admin/{name}/...` | `/admin/shop/order/save` |
| WebSocket event | `{name}.xxx` | `shop.orderNotify` |
| Queue topic | `{name}_xxx` | `shop_order_process` |
| Cron task name | `{name}_xxx` | `shop_expire_check` |

### 11.3 Go 包路径

扩展代码的 Go import 路径为 `xygo/addons/{name}`（在项目根 module `xygo` 下自动可用）。

### 11.4 禁止事项

1. **禁止**将文件放到 `server/internal/`、`server/api/`、`web/src/views/` 等核心目录
2. **禁止**修改 `cmd.go`、`main.go` 等核心入口文件
3. **禁止**在 SQL 中对核心表执行 ALTER/DROP/UPDATE
4. **禁止**注册与核心或其他扩展冲突的菜单 name / 路由 path / 配置 key
5. 扩展间若有依赖，在 `addon.yaml` 中用 `dependencies` 字段声明

### 11.5 打包工具

集成到 `tools.go addon pack`，自动：
1. 读取 `addon.yaml`，校验命名规范
2. 收集 `server/addons/{name}/` → ZIP 内 `server/`
3. 收集 `web/src/addons/{name}/` → ZIP 内 `web/`
4. 收集 `install/`、`uninstall/`、`upgrade/` SQL
5. **校验 SQL 中的表名前缀和菜单标记**
6. 生成 ZIP

## 十二、扩展脚手架 — `addon create`

### 10.1 为什么需要脚手架

扩展开发者不可能在不熟悉框架约定的情况下从零手写出所有文件。
需要一个命令行工具，一键生成**可直接运行的标准扩展模板**，开发者在此基础上修改。

> GoFrame 本身没有扩展/插件脚手架概念（`gf init` 只能创建完整项目，`gf gen` 只生成 dao/service）。
> 这个工具完全由我们自己实现，集成到 `tools.go` 中。

### 10.2 命令入口

```bash
# 在 server/ 目录下执行
go run tools.go addon create         # 交互式
go run tools.go addon create myext   # 指定扩展名
```

`tools.go` 新增 `create` 子命令：

```go
case "addon":
    switch sub {
    case "create":
        // 调用 addon.Create(ctx, name)
    case "install":
        // ...
    case "uninstall":
        // ...
    case "pack":
        // 调用 addon.Pack(ctx, name) — 打包工具
    }
```

### 10.3 交互式创建流程

```
$ go run tools.go addon create

  ╔══════════════════════════════════════╗
  ║  XYGo Admin 扩展脚手架               ║
  ╚══════════════════════════════════════╝

  扩展标识 (英文小写，如 shop):  shop
  扩展名称 (中文，如 商城管理):  商城管理
  作者:  开发者A
  描述:  在线商城与订单管理
  是否包含独立前端 (y/N):  n
  示例表名 (如 shop_order，留空跳过):  shop_order

  [1/5] 创建后端目录结构 ... OK
  [2/5] 生成模板文件 ... OK (12 个文件)
  [3/5] 创建前端目录结构 ... OK
  [4/5] 生成 SQL 模板 ... OK
  [5/5] 更新 cmd_addons.go ... OK

  ════════════════════════════════════════
  扩展 shop 创建完成！
  ════════════════════════════════════════

  生成的文件：
    server/addons/shop/
    ├── addon.yaml
    ├── module.go
    ├── api/shop_order.go
    ├── controller/shop_order.go
    ├── logic/shop_order.go
    ├── model/shop_order.go
    └── service/shop_order.go

    web/src/addons/shop/
    ├── manifest.json
    ├── views/order/index.vue
    ├── views/order/modules/order-dialog.vue
    └── api/order.ts

    server/addons/shop/install/
    ├── pgsql.sql
    └── mysql.sql

    server/addons/shop/uninstall/
    ├── pgsql.sql
    └── mysql.sql

  下一步：
    1. 编辑 install/pgsql.sql 定义你的表结构
    2. 执行安装 SQL：go run tools.go addon install shop
    3. gf gen dao （生成数据模型）
    4. 修改 controller/logic 实现业务逻辑
    5. 启动后端，访问页面验证
```

### 10.4 生成的模板文件详解

#### addon.yaml（元信息）

```yaml
name: shop
version: "1.0.0"
title: "商城管理"
description: "在线商城与订单管理"
author: "开发者A"
min_version: "1.3.0"

features:
  routes: true
  websocket: false
  queue: false
  cron: false
```

#### module.go（模块注册入口）

```go
package shop

import (
    "xygo/internal/addon"
    "xygo/internal/middleware"
    "xygo/addons/shop/controller"

    "github.com/gogf/gf/v2/net/ghttp"
)

func init() {
    addon.Register(addon.Module{
        Name: "shop",
        Mount: func(s *ghttp.Server) {
            s.Group("/", func(group *ghttp.RouterGroup) {
                group.Middleware(
                    middleware.CORS,
                    middleware.ResponseHandler,
                    middleware.AdminAuth,
                    middleware.DemoGuard,
                )
                group.Bind(controller.NewV1())
            })
        },
    })

    // 如需 WebSocket 事件：
    // websocket.RegisterEvent("shop.orderNotify", handleOrderNotify)

    // 如需消息队列：
    // queue.Register(&OrderProcessConsumer{})

    // 如需定时任务：
    // cron.Register(&OrderTimeoutTask{})
}
```

#### api/shop_order.go（API 请求/响应定义）

```go
package api

import "github.com/gogf/gf/v2/frame/g"

// 列表
type ShopOrderListReq struct {
    g.Meta `path:"/admin/shop/order/list" method:"get" tags:"商城-订单" summary:"订单列表"`
    Page   int `json:"page" d:"1"`
    Size   int `json:"size" d:"20"`
}
type ShopOrderListRes struct {
    g.Meta `mime:"application/json"`
    List   interface{} `json:"list"`
    Total  int         `json:"total"`
}

// 保存
type ShopOrderSaveReq struct {
    g.Meta `path:"/admin/shop/order/save" method:"post" tags:"商城-订单" summary:"保存订单"`
    Id     uint64 `json:"id"`
    // TODO: 添加业务字段
}
type ShopOrderSaveRes struct{}

// 删除
type ShopOrderDeleteReq struct {
    g.Meta `path:"/admin/shop/order/delete" method:"post" tags:"商城-订单" summary:"删除订单"`
    Id     uint64 `json:"id" v:"required"`
}
type ShopOrderDeleteRes struct{}
```

#### controller/shop_order.go（控制器模板）

```go
package controller

import (
    "context"
    api "xygo/addons/shop/api"
)

type ControllerV1 struct{}

func NewV1() *ControllerV1 { return &ControllerV1{} }

func (c *ControllerV1) ShopOrderList(ctx context.Context, req *api.ShopOrderListReq) (res *api.ShopOrderListRes, err error) {
    // TODO: 调用 logic 获取列表
    res = &api.ShopOrderListRes{}
    return
}

func (c *ControllerV1) ShopOrderSave(ctx context.Context, req *api.ShopOrderSaveReq) (res *api.ShopOrderSaveRes, err error) {
    // TODO: 调用 logic 保存
    return
}

func (c *ControllerV1) ShopOrderDelete(ctx context.Context, req *api.ShopOrderDeleteReq) (res *api.ShopOrderDeleteRes, err error) {
    // TODO: 调用 logic 删除
    return
}
```

#### install/pgsql.sql（安装 SQL 模板）

```sql
-- ============================================================
-- 扩展: shop (商城管理)
-- 安装 SQL - PostgreSQL
-- ============================================================

-- 1. 建表
CREATE TABLE IF NOT EXISTS xy_shop_order (
    id         bigserial PRIMARY KEY,
    title      varchar(255) NOT NULL DEFAULT '',
    status     smallint     NOT NULL DEFAULT 1,
    sort       int          NOT NULL DEFAULT 0,
    created_by bigint       NOT NULL DEFAULT 0,
    updated_by bigint       NOT NULL DEFAULT 0,
    created_at bigint       NOT NULL DEFAULT 0,
    updated_at bigint       NOT NULL DEFAULT 0,
    deleted_at bigint       NOT NULL DEFAULT 0
);

CREATE INDEX IF NOT EXISTS idx_shop_order_deleted_at ON xy_shop_order(deleted_at);
CREATE INDEX IF NOT EXISTS idx_shop_order_status ON xy_shop_order(status);

COMMENT ON TABLE xy_shop_order IS '商城-订单表';

-- 2. 插入后台菜单（remark 标记 addon:shop 用于卸载时批量删除）
INSERT INTO xy_admin_menu (parent_id, type, title, name, path, component, icon, hidden, keep_alive, sort, status, perms, remark, create_time, update_time)
VALUES (0, 2, '商城管理', 'ShopManage', 'shop', '@addons/shop/views/order', 'ri:shopping-cart-line', 0, 1, 60, 1, '', 'addon:shop',
        EXTRACT(EPOCH FROM NOW())::int, EXTRACT(EPOCH FROM NOW())::int);

-- 子菜单（列表页）
INSERT INTO xy_admin_menu (parent_id, type, title, name, path, component, icon, hidden, keep_alive, sort, status, perms, remark, create_time, update_time)
VALUES (currval('xy_admin_menu_id_seq'), 2, '订单管理', 'ShopOrderList', 'order', '@addons/shop/views/order', '', 0, 1, 1, 1, '', 'addon:shop',
        EXTRACT(EPOCH FROM NOW())::int, EXTRACT(EPOCH FROM NOW())::int);

-- 按钮权限
INSERT INTO xy_admin_menu (parent_id, type, title, name, path, component, icon, hidden, keep_alive, sort, status, perms, remark, create_time, update_time)
VALUES (currval('xy_admin_menu_id_seq'), 3, '新增/编辑', 'ShopOrderSave', '', '', '', 0, 0, 1, 1, '/admin/shop/order/save', 'addon:shop',
        EXTRACT(EPOCH FROM NOW())::int, EXTRACT(EPOCH FROM NOW())::int);
```

#### uninstall/pgsql.sql（卸载 SQL 模板）

```sql
-- 扩展: shop 卸载
-- 1. 删除菜单（通过 remark 标记批量删除）
DELETE FROM xy_admin_menu WHERE remark = 'addon:shop';

-- 2. 删除数据表（谨慎：会丢失所有数据）
DROP TABLE IF EXISTS xy_shop_order;
```

#### web/src/addons/shop/views/order/index.vue（前端列表页模板）

模板继承系统已有的代码生成风格（ArtTable + ArtSearchBar + 对话框模式），
生成一个可运行的标准 CRUD 列表页。

#### web/src/addons/shop/api/order.ts（前端 API 模板）

```typescript
import { adminRequest } from '@/utils/http'

const prefix = '/admin/shop/order'

export function getOrderList(params: any) {
    return adminRequest.get(`${prefix}/list`, { params })
}

export function saveOrder(data: any) {
    return adminRequest.post(`${prefix}/save`, data)
}

export function deleteOrder(data: any) {
    return adminRequest.post(`${prefix}/delete`, data)
}
```

### 10.5 模板引擎实现方式

模板文件使用 Go 的 `text/template` 或 `embed` 内嵌。变量包括：

| 变量 | 示例值 | 用途 |
|------|-------|------|
| `{{.Name}}` | `shop` | 扩展标识 |
| `{{.Title}}` | `商城管理` | 中文名称 |
| `{{.Author}}` | `开发者A` | 作者 |
| `{{.Description}}` | `在线商城与订单管理` | 描述 |
| `{{.TableName}}` | `shop_order` | 示例表名 |
| `{{.TablePrefix}}` | `xy_` | 表前缀 |
| `{{.ModulePath}}` | `xygo/addons/shop` | Go import 路径 |
| `{{.PascalName}}` | `ShopOrder` | 大驼峰（用于结构体名） |
| `{{.CamelName}}` | `shopOrder` | 小驼峰（用于变量名） |
| `{{.KebabName}}` | `shop-order` | 短横线（用于前端路由） |

模板文件存放位置：`server/resource/addon_template/`

```
server/resource/addon_template/
├── addon.yaml.tpl
├── module.go.tpl
├── api.go.tpl
├── controller.go.tpl
├── logic.go.tpl
├── model.go.tpl
├── service.go.tpl
├── install_pgsql.sql.tpl
├── install_mysql.sql.tpl
├── uninstall_pgsql.sql.tpl
├── uninstall_mysql.sql.tpl
├── web_manifest.json.tpl
├── web_index.vue.tpl
├── web_dialog.vue.tpl
└── web_api.ts.tpl
```

> 注意：这套模板与 `server/resource/generate/default/` 下的 CRUD 代码生成模板
> 是**不同的用途**。generate 模板用于在已有系统中按数据库表生成页面；
> addon_template 用于创建扩展包骨架。两者风格保持一致但独立维护。

### 10.6 与 gf gen 的配合

扩展创建完成后，开发者的标准工作流：

```bash
# 1. 创建扩展骨架
go run tools.go addon create shop

# 2. 编辑 install SQL，定义表结构
#    然后执行 SQL 建表

# 3. 生成 DAO（GoFrame 命令，从数据库表生成）
gf gen dao -t xy_shop_order

# 4. 在 logic/ 中编写业务逻辑，引用生成的 dao
# 5. 在 controller/ 中调用 logic
# 6. 前端修改 views/ 页面
# 7. 测试
# 8. 打包
go run tools.go addon pack shop
```

`gf gen dao` 生成的 dao/model/entity 文件仍然在核心目录（`internal/dao/`、`internal/model/`），
这不影响隔离性——它们是数据库 schema 的自动映射，每次 `gf gen dao` 都会重新生成。

## 十三、CRUD 代码生成器扩展支持

### 13.1 问题

现有代码生成器（gen-codes）只能将代码生成到主包目录：

```yaml
# config.yaml 当前配置
xygen:
  application:
    crud:
      templates:
        - group: "default"
          apiPath: "api/admin"
          controllerPath: "internal/controller/admin"
          logicPath: "internal/logic"
          inputPath: "internal/model/input/adminin"
          webApiPath: "../web/src/api/backend"
          webViewsPath: "../web/src/views/backend"
```

扩展开发者用代码生成器生成 CRUD 后，文件会落到核心目录，违反隔离原则。

### 13.2 方案：动态注册扩展模板组

`xygen.application.crud.templates` 已经是数组结构，天然支持多组模板。
扩展安装时自动追加一组以 `addon_{name}` 为 group 的模板配置。

但更好的方式是**不改 config.yaml**，而是代码生成器**运行时自动扫描已安装扩展**，
动态构建可选模板组列表：

```go
// gencodes 启动时扫描 server/addons/*/addon.yaml
func getAvailableTemplateGroups(ctx context.Context) []TemplateGroup {
    groups := []TemplateGroup{
        genconfig.GetDefaultTemplate(ctx),  // 主包（default）
    }

    addonsDir := filepath.Join(gfile.Pwd(), "addons")
    dirs, _ := os.ReadDir(addonsDir)
    for _, d := range dirs {
        if !d.IsDir() { continue }
        addonYaml := filepath.Join(addonsDir, d.Name(), "addon.yaml")
        if !gfile.Exists(addonYaml) { continue }

        name := d.Name()
        groups = append(groups, TemplateGroup{
            Group:          "addon_" + name,
            Label:          "扩展: " + name,     // 前端下拉显示
            TemplatePath:   "./resource/generate/default", // 复用同一套模板
            ApiPath:        fmt.Sprintf("addons/%s/api", name),
            ControllerPath: fmt.Sprintf("addons/%s/controller", name),
            LogicPath:      fmt.Sprintf("addons/%s/logic", name),
            InputPath:      fmt.Sprintf("addons/%s/model", name),
            SqlPath:        fmt.Sprintf("addons/%s/install", name),
            WebApiPath:     fmt.Sprintf("../web/src/addons/%s/api", name),
            WebViewsPath:   fmt.Sprintf("../web/src/addons/%s/views", name),
        })
    }
    return groups
}
```

### 13.3 前端 UI 变更

代码生成器页面新增一个**「生成目标」**下拉选择器（放在模块名输入之上）：

```
┌─────────────────────────────────────────┐
│ 生成目标:  [▾ 主包 (default)         ]  │
│            ├── 主包 (default)           │
│            ├── 扩展: shop              │
│            └── 扩展: tenant            │
│                                         │
│ 关联表:    xy_shop_order                │
│ 模块名:    商城订单                      │
│ ...                                     │
└─────────────────────────────────────────┘
```

选择扩展目标后：
- 输出路径自动切换到扩展目录
- 菜单 SQL 的 `remark` 自动填入 `addon:{name}`
- 表名自动校验 `xy_{name}_` 前缀
- API 路径前缀自动调整（如 `/admin/shop/order/list`）

### 13.4 后端生成逻辑调整

`getTplFiles` 根据选择的 template group 使用对应路径：

```go
func getTplFiles(ctx context.Context, data *TplData) []tplFile {
    // tpl 不再写死 genconfig.GetDefaultTemplate
    // 而是根据 data.TargetGroup 获取对应模板配置
    tpl := genconfig.GetTemplateByGroup(ctx, data.TargetGroup)

    files := []tplFile{
        {TplName: "api.go.tpl",        OutPath: fmt.Sprintf("%s/%s_%s.go", tpl.ApiPath, data.AddonPrefix, snakeName), ...},
        {TplName: "controller.go.tpl", OutPath: fmt.Sprintf("%s/%s.go", tpl.ControllerPath, snakeName), ...},
        {TplName: "logic.go.tpl",      OutPath: fmt.Sprintf("%s/%s/%s.go", tpl.LogicPath, snakeName, snakeName), ...},
        // ...
    }
    return files
}
```

### 13.5 模板适配

现有 `.tpl` 模板文件基本可以复用，但需要新增几个模板变量以支持扩展场景：

| 新增变量 | 说明 | 主包值 | 扩展值 |
|---------|------|--------|-------|
| `{{.TargetGroup}}` | 目标模板组 | `default` | `addon_shop` |
| `{{.AddonName}}` | 扩展名（空=主包） | `""` | `shop` |
| `{{.MenuRemark}}` | 菜单 remark 标记 | `""` | `addon:shop` |
| `{{.ImportBase}}` | Go import 基路径 | `xygo/internal` | `xygo/addons/shop` |
| `{{.ApiPrefix}}` | API 路由前缀 | `/admin` | `/admin/shop` |

模板中需要适配的关键处（示例）：

**api.go.tpl** — import 路径：

```
package api

import "github.com/gogf/gf/v2/frame/g"

// ...
type {{.PascalName}}ListReq struct {
    g.Meta `path:"{{.ApiPrefix}}/{{.KebabName}}/list" method:"get"`
    // ...
}
```

**menu.sql.tpl** — remark 标记：

```sql
INSERT INTO xy_admin_menu (..., remark, ...)
VALUES (..., '{{.MenuRemark}}', ...);
```

**controller.go.tpl** — import 路径：

```go
package controller

import (
    api "{{.ImportBase}}/api"
    // ...
)
```

### 13.6 代码生成器的删除/回滚

现有的"删除已生成代码"功能也需适配：当删除一个扩展内生成的 CRUD 时，
清理的是扩展目录下的文件，而不是核心目录。

`generatedFiles` 记录的是绝对路径，所以删除逻辑本身不需要改，
只要生成时记录了正确的扩展目录路径即可。

菜单回滚同理——通过已记录的菜单 ID 或 `remark` 标记删除。

### 13.7 工作流示例

扩展开发者的 CRUD 开发流程：

```bash
# 1. 创建扩展骨架
go run tools.go addon create shop

# 2. 编辑 install SQL，定义 xy_shop_order 表结构
# 3. 执行 SQL 建表

# 4. 生成 DAO（GoFrame 标准命令）
gf gen dao -t xy_shop_order

# 5. 打开代码生成器页面
#    - 生成目标: 选择「扩展: shop」
#    - 关联表:   xy_shop_order
#    - 生成

# 6. 代码自动生成到：
#    server/addons/shop/api/shop_order.go
#    server/addons/shop/controller/shop_order.go
#    server/addons/shop/logic/shop_order/shop_order.go
#    server/addons/shop/model/shop_order.go
#    web/src/addons/shop/views/order/index.vue
#    web/src/addons/shop/views/order/modules/order-dialog.vue
#    web/src/addons/shop/api/order.ts

# 7. 修改完善业务逻辑
# 8. 测试
# 9. 打包
go run tools.go addon pack shop
```

## 十四、迁移计划（现有租户扩展）

### 需要迁移的文件

| 当前位置 | 迁移到 |
|---------|-------|
| `server/api/admin/admin_tenant*.go` | `server/addons/tenant/api/` |
| `server/api/tenant/*.go` | `server/addons/tenant/api/` |
| `server/internal/controller/admin/tenant*.go` | `server/addons/tenant/controller/` |
| `server/internal/controller/tenant/*.go` | `server/addons/tenant/controller/` |
| `server/internal/logic/tenant/*.go` | `server/addons/tenant/logic/` |
| `server/internal/logic/tenantgroup/*.go` | `server/addons/tenant/logic/` |
| `server/internal/model/input/adminin/tenant*.go` | `server/addons/tenant/model/` |
| `server/internal/model/input/tenantin/*.go` | `server/addons/tenant/model/` |
| `server/internal/service/tenant*.go` | `server/addons/tenant/service/` |
| `web/src/views/backend/tenant/` | `web/src/addons/tenant/views/` |
| `web/src/api/backend/tenant/` | `web/src/addons/tenant/api/` |
| `web-tenant/` | 不变 |

### 不需要迁移的

- `gf gen dao/model/entity` 生成的文件：继续在核心目录，自动维护
- `web-tenant/`：已是独立目录
- `cmd.go` 中预埋的租户路由组：迁移后由 `module.go` 接管，删除预埋代码

### 迁移步骤

1. 创建 `server/addons/tenant/` 目录结构
2. 移动文件，调整 Go package 声明和 import 路径
3. 编写 `module.go` 注册入口
4. 删除 `cmd.go` 中的租户路由预埋代码
5. 创建 `cmd_addons.go` 空导入
6. 编译验证
7. 重新打包 `tenant.zip`

## 十五、工具命令速查

```bash
# 在 server/ 目录下
go run tools.go addon create         # 交互式创建扩展骨架
go run tools.go addon create shop    # 创建名为 shop 的扩展
go run tools.go addon install shop   # 安装扩展
go run tools.go addon uninstall shop # 卸载扩展
go run tools.go addon pack shop      # 打包扩展为 ZIP
go run tools.go addon list           # 列出已安装的扩展
```

## 十六、实施优先级

| 阶段 | 内容 | 影响范围 | 状态 |
|------|------|---------|------|
| P0 | 创建 `addon/registry.go`，`cmd.go` 加 `MountAll` | 核心新增 1 个包 + 1 行代码 | 已完成 |
| P1 | 前端 `ComponentLoader` 支持 `@addons/` 前缀 | 改 1 个文件 | 已完成 |
| P2 | 改造 addon installer（隔离式路径 + addons.go 维护） | 改 `addon.go` | 已完成 |
| P2.5 | **扩展脚手架 `addon create`** + 声明式菜单 + Auth Endpoint Factory | 新增 `create.go` `menu.go` `token/endpoint.go` | 已完成 |
| P3 | **CRUD 生成器支持扩展目标**（动态扫描 + UI 下拉 + 路径适配） | 改 `generate.go` + 前端 gen-codes 页面 | 已完成 |
| P4 | 迁移租户扩展到新结构（含完整核心解耦） | 文件移动 + package 调整 + 核心去租户硬编码 | 已完成 |
| P5 | 升级包 SKIP_PATTERNS + updater 扩展目录保护 | `pack.py` + `updater.go` | 已完成 |
| P6 | **扩展打包工具 `addon pack`** | 新增 `pack.go` + `tools.go` | 已完成 |
| 升级机制 | MinUpgradeFrom 校验 + 升级前备份 + upgrade SQL 幂等模板 | `addon.go` + `create.go` | 已完成 |
